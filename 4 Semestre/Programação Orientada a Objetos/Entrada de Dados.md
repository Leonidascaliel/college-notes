### Como criar uma entrada de dados
Para fazer isso é preciso criar um objeto. Utiliza-se a classe "**Scanner**"
	
	Scanner entrada = new Scanner(System.in);

	nextInt() = Sequencia de dados Int
	nextDouble() = Sequencia de dados Double
	next()